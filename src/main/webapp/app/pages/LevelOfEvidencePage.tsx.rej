diff a/src/main/webapp/app/pages/LevelOfEvidencePage.tsx b/src/main/webapp/app/pages/LevelOfEvidencePage.tsx	(rejected hunks)
@@ -192,16 +194,16 @@ export default class LevelOfEvidencePage extends React.Component<
-                    <Form.Group>
-                      {[Version.FDA, Version.AAC].map(version => (
-                        <Form.Check
-                          label={`Show mapping to ${LEVEL_NAME[version]}`}
-                          type="checkbox"
-                          onClick={() =>
-                            this.toggleVersion(
-                              this.version === version ? Version.V2 : version
-                            )
-                          }
-                          name="mapping-to-other-levels"
-                          id={`mapping-to-other-levels-${version}`}
-                          checked={this.version === version}
-                        />
-                      ))}
-                    </Form.Group>
+                  <Form.Group>
+                    {[Version.FDA, Version.AAC].map(version => (
+                      <Form.Check
+                        label={`Show mapping to ${LEVEL_NAME[version]}`}
+                        type="checkbox"
+                        onClick={() =>
+                          this.toggleVersion(
+                            this.version === version ? Version.V2 : version
+                          )
+                        }
+                        name="mapping-to-other-levels"
+                        id={`mapping-to-other-levels-${version}`}
+                        checked={this.version === version}
+                      />
+                    ))}
+                  </Form.Group>
